import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function CertificationsPage() {
  return (
    <PlaceholderSection
      title="Certifications & Affiliations"
      description="Trust badges — certifications and industry affiliations — list + create + edit + delete."
      schemaTypes={["certification"]}
    />
  )
}
