import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function PromotionsPage() {
  return (
    <PlaceholderSection
      title="Promotions & Specials"
      description="Marketing promotions and special offers — list + create + edit + delete."
      schemaTypes={["promotion"]}
    />
  )
}
