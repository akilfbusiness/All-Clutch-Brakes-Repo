import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function GalleryPage() {
  return (
    <PlaceholderSection
      title="Gallery"
      description="Workshop/gallery images — list + upload + edit + delete."
      schemaTypes={["galleryImage"]}
    />
  )
}
