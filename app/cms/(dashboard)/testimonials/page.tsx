import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function TestimonialsPage() {
  return (
    <PlaceholderSection
      title="Testimonials"
      description="Customer testimonials/reviews — list + create + edit + delete."
      schemaTypes={["testimonial"]}
    />
  )
}
