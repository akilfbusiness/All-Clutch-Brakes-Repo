import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function NavigationPage() {
  return (
    <PlaceholderSection
      title="Navigation"
      description="Site header/footer navigation menu — a singleton document (documentId: mainNavigation)."
      schemaTypes={["navigation"]}
    />
  )
}
