import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function LocationsPage() {
  return (
    <PlaceholderSection
      title="Locations"
      description="Individual location/branch pages — list + create + edit + delete."
      schemaTypes={["location"]}
    />
  )
}
