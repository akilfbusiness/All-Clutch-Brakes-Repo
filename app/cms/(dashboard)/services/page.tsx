import { PlaceholderSection } from "@/components/cms/placeholder-section"

export default function ServicesPage() {
  return (
    <PlaceholderSection
      title="Services"
      description="Individual service pages (e.g. Clutch Replacement, Brake Repairs) — list + create + edit + delete."
      schemaTypes={["service"]}
    />
  )
}
